# Development / test dependencies (Track α — backend safety net)
# Runtime deps live in requirements.txt; httpx is already declared there.
pytest==8.3.3
pytest-asyncio==0.24.0
asgi-lifespan==2.1.0
ruff==0.6.9
